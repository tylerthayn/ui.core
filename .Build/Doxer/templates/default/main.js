require(['jquery', './js/Docs'], ($, Docs) => {

/*
	$(() => {
		ast.Filter({kind: 'class'}).forEach(doclet => {
			Docs.TOC.InsertClass(doclet)
			ast.Filter({memberof: doclet.longname, scope: 'static', kind: 'function'}).forEach(d => {
				Docs.TOC.InsertClassMember(doclet, d)
			})
			ast.Filter({memberof: doclet.longname, scope: 'static', kind: 'member'}).forEach(d => {
				Docs.TOC.InsertClassMember(doclet, d)
			})
			ast.Filter({memberof: doclet.longname, scope: 'static', kind: 'param,'}).forEach(d => {
				Docs.TOC.InsertClassMember(doclet, d)
			})

			ast.Filter({memberof: doclet.longname, scope: 'instance', kind: 'function'}).forEach(d => {
				Docs.TOC.InsertClassMember(doclet, d)
			})
			ast.Filter({memberof: doclet.longname, scope: 'instance', kind: 'member'}).forEach(d => {
				Docs.TOC.InsertClassMember(doclet, d)
			})
			ast.Filter({memberof: doclet.longname, scope: 'instance', kind: 'para,'}).forEach(d => {
				Docs.TOC.InsertClassMember(doclet, d)
			})
			ast.Filter({memberof: doclet.longname, scope: 'instance', kind: 'event'}).forEach(d => {
				Docs.TOC.InsertClassMember(doclet, d)
			})


		})

		//Docs.TOC.Insert(docs)
		//logj(docs)

	})
*/
})
